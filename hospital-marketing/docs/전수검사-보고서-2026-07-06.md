# 전수검사 보고서 — 2026-07-06

검사 범위: `hospital-marketing/` 전체 (HTML 12, 파이프라인 py 11, 문서, 스키마) + 베놈 사이트(desktop-tutorial) 챗봇 연결
검사 방식: `/검사` 스킬 절차 — 자동 스크립트 검증 + Playwright 실측 렌더(12페이지 × 데스크톱/모바일) + 정밀 문구 검토
표기: ✅ 통과 · 🔧 이번 검사에서 수정 · ⚠️ 결정 필요
특별 오더: **베놈 체크업 허브에 챗봇 누락 → 등재 완료** + 허브 누락 전수 대조 + 원장님 앱 진단/결과 화면 검증

## 요약

**10개 영역 중 7개 통과, 3개 영역에서 경미한 문제 발견 → 전부 즉시 수정(🔧 14건).**
핵심 성과: ① 고아 모듈이던 **의료광고심의 도우미 챗봇**을 허브에 등재하고 베놈 사이트
의료광고심의 페이지에도 진입점 신설, ② 콘솔 에러 0건 달성, ③ 금지 용어("경쟁력 점수") 잔재 완전 제거.

## 특별 오더 1 — 허브 도구 전수 대조 (누락 점검)

| 도구 | 실체 위치 | 허브 카드 | 판정 |
|---|---|---|---|
| 원클릭 검진 | landing/ | ✅ 기존 | 정상 |
| 종합검진 | dashboard/ | ✅ 기존 | 정상 |
| 리스크 진단 | self-check/#risk | ✅ 기존 | 정상 (해시 딥링크 동작 확인) |
| 비용 진단 | self-check/#cost | ✅ 기존 | 정상 |
| 동선 진단 | self-check/#journey | ✅ 기존 | 정상 |
| 입지 진단 | location/ | ✅ 기존 | 정상 |
| **심의 챗봇 (AI)** | 베놈 사이트 `/chatbot/demo.html` | 🔧 **이번에 등재** | **유일한 누락이었음** |
| 검진 신청 | landing/ | ✅ 상·하단 CTA | 정상 (카드 아닌 CTA — 의도) |
| 차트룸(관리자단) | admin/ | — | 내부용, 비등재가 맞음 (noindex) |
| 월간 리포트 | report/ | — | 내부 발송 템플릿, 비등재가 맞음 |
| 정밀 리포트 샘플 | reports/ | — | 내부 검증용 (noindex,nofollow) |

**결론: 챗봇 1건 외 누락 없음.** 챗봇 조치 내역:
1. 허브(index.html) 7번째 카드 "심의 챗봇" 추가 — AI 태그, 외부 링크(target=_blank rel=noopener)
2. self-check 푸터 "다른 검진 도구"에 심의 챗봇 링크 추가
3. 베놈 사이트 의료광고심의 페이지(`ld('shimui')`) 하단에 챗봇 진입 섹션(`#ds-chatbot`) 신설 + 면책 고지
4. 도구체계 문서(브랜딩-베놈체크업-도구체계)에 심의 챗봇 행 추가
5. 챗봇 자체 상태 검증: 오프라인 스모크 테스트 **7/7 통과** (RAG 검색·문구 진단·대체표현 생성 정상)

## 특별 오더 2 — 원장님 앱 진단/결과 화면

- **종합검진 대시보드**: `data/dashboard-live.json` **실데이터**(강남 피부과 실측)로 렌더 확인.
  진단 점수 34점 + 항목 기여도 + 반경별 비교 + 액션 3가지 정상. 콘솔 에러 0.
- **결과 화면**: 액션 리포트 탭 + 정밀 진단 리포트(reports/venom-daegu-test.html, 실데이터 6건 노출 검증) 정상.
- **리스크 진단 실행 흐름**: 예시 문구 → 위험 3종·주의 3종 하이라이트 + 조항 근거 + 순화 예시 정상 동작.
- 🔧 발견·수정: 실데이터 로드 시에도 푸터가 "목업 데이터 기반"으로 남는 모순 → 로더가 "실데이터 기반"으로 갱신하도록 수정.
- 스크린샷 7종(라이트/다크·탭별) 대표님께 전달.

## 영역별 결과

| 영역 | 상태 | 비고 |
|---|---|---|
| 1. SEO | ✅ | 12페이지 실측: title 24~35자, desc 적정, JSON-LD 전건 파싱 통과, h1 구조 정상(🔧 dashboard h1 부여), 내부 페이지 noindex 유지, sitemap(landing만 공개)·robots 일치. 허브 noindex는 의도(공개 진입은 landing) — 서브도메인 이전 시 재검토 |
| 2. 의료법 문구 | 🔧 | 금지 표현 매칭 전수 문맥 확인 → 위반 0건(전부 부정 고지·룰엔진 정의·예시). 필수 고지(내부 참고용·전재 금지·당사 산식·보장 없음·출처/기준일) 3개 리포트 모두 존재. "미노출=API 응답 상위 5건 밖" 정의 표기 확인. 🔧 산식 표기 통일: "자사 산식"→"당사 산식"(daegu 리포트) |
| 3. 콘텐츠 조건 | ✅ | 신청폼 필수 2(required+JS 이중검사)/선택 분리 정상 — **필수만 체크해도 제출 가능 실측 확인**. 점수 명칭 "검색정보 운영 진단 점수" 통일. ⚠️ 동의 체크박스 c1~c4에 name 속성 없음 — 서버 연동 시 동의 증적 저장 보완 필요(아래 ⚠️-2) |
| 4. 오탈자·일관성 | 🔧 | 🔧 금지 용어 "경쟁력 점수" 3건(dashboard 차트 제목·aria-label·툴팁) → "진단 점수". 🔧 구명칭 "경쟁력" 잔재: dashboard meta desc, 월간 리포트 title·본문("중상위권"→"양호 등급") 수정. 🔧 VENOMAD/venomad 혼용 3건 → 소문자 통일. 🔧 daegu 리포트 6건/5건 표기 불일치 수정. 🔧 "2개월차"→"2개월 차", 지도 안내 문구, GA4 예시 자릿수 통일. 메디랭크 잔재 0건 |
| 5. 링크 매칭 | ✅ | 12개 HTML 상대 링크·앵커·fetch 경로 스크립트 검증 — 깨진 링크 0건, 비HTTPS 외부 링크 0건 |
| 6. 이미지·webP | ✅ | 래스터 이미지 0장, `<img>` 태그 0개 — 전 화면 인라인 SVG/CSS 벡터. webP 변환 대상 없음 |
| 7. 코드 최적화 | 🔧 | 파이프라인 단위 테스트 13건 통과. 🔧 목업 데이터 모순 수정: "상위 5건 API" 기준인데 7·9·12위 값 존재 → 5 이내로 정정. 페이지별 esc()/팔레트 중복은 단일 파일 원칙상 현행 유지(페이지 8개 초과 시 추출 권장 — 현재 12페이지지만 리포트류 제외 시 7개) |
| 8. UI/UX | 🔧 | 12페이지 × 데스크톱(1200px)/모바일(390px) = 24조합 실측: **콘솔 에러 0건 달성**. 🔧 meta CSP의 frame-ancestors 지시어(브라우저가 meta에서 무시·콘솔 에러 유발) 9개 파일에서 제거. 🔧 favicon 부재 404 → 인라인 SVG favicon 12개 파일 추가. 다크모드 렌더 정상. 월간 리포트 모바일 가로 스크롤은 A4(210mm) 인쇄 템플릿의 의도된 특성 |
| 9. 보안 | ✅ | 사용자 입력 innerHTML 삽입 지점 전수 확인 — 전건 esc() 이스케이프 적용(리스크 진단 하이라이트 포함). CSP 전 페이지 존재, .env 미커밋(.env.example만), 관리자단 프로토타입 표기 유지 |
| 10. 처리방침·제공조건 | ⚠️ | 제공조건 7개 항목 존재, 법무 문서 간 모순 없음. 처리방침 placeholder 13개 미기입 상태 지속(아래 ⚠️-1) |

## 🔧 이번 검사에서 수정한 것 (14건)

1. **허브에 "심의 챗봇" 카드 등재** (index.html) — 챗봇 아이콘 SVG + 외부 링크 처리
2. self-check 푸터 도구 목록에 심의 챗봇 추가
3. 베놈 사이트(desktop-tutorial) 의료광고심의 페이지에 챗봇 진입 섹션 + 면책 고지 (SPA 렌더·JS 에러 0 검증)
4. 도구체계 문서에 심의 챗봇 행 추가
5. meta CSP `frame-ancestors` 제거 (9개 파일 — 콘솔 에러 원인)
6. 인라인 SVG favicon 추가 (12개 파일 — /favicon.ico 404 제거)
7. dashboard h1 부여 (브랜드 헤더를 h1으로)
8. 금지 용어 "경쟁력 점수" → "진단 점수" (dashboard 3곳)
9. 구명칭 "경쟁력" 잔재 정리 (dashboard meta, 월간 리포트 title·본문 등급어 "중상위권"→"양호")
10. 실데이터 로드 시 푸터 "목업 데이터" 문구 모순 수정 (dashboard)
11. 목업 키워드 위치값 모순 수정 (상위 5건 기준인데 7·9·12위 → 4·5·5)
12. VENOMAD → venomad 대소문자 통일 (월간 리포트·daegu 리포트 3곳)
13. daegu 리포트 "노출 확인 5건"→"6건" 정합, "자사 산식"→"당사 산식"
14. 띄어쓰기·문구: "2개월 차", 지도 "마우스를 올리면", admin GA4 예시 `G-XXXXXXXXXX`

## ⚠️ 결정 필요 (대표님)

1. **처리방침 placeholder 13개** — 시행일, 수탁자 4곳, 보호책임자 성명·연락처. 업체 확정 시 기입해야 게시 가능 (전 회차와 동일, 미해결).
2. **신청폼 동의 체크박스 name 속성 없음** — 현재 정적 페이지라 무해하나, 접수 서버 연동 시 동의 여부(c1~c4)가 전송 데이터에 담기도록 보완 필요. 서버 연동 결정과 함께 진행 권장.
3. **베놈 사이트 무료 도구(SEO 점수·GEO 점수·키워드 검색량 체크)의 허브 통합 여부** — 별도 사이트(venomad 마케팅 사이트) 소속 도구를 체크업 허브에도 노출할지. 챗봇처럼 외부 링크 카드로 추가 가능하나, 허브 정체성(검진 5+2종)과 퍼널 설계에 영향 → 브랜드 판단 필요.
4. **챗봇 배포 URL** — 현재 허브 카드는 `desktop-tutorial-chi-peach.vercel.app`을 가리킴. `diagnosis.venomad.com` 등 커스텀 도메인 이전(통합 방안 문서 A안) 시 허브 카드·self-check 푸터·심의 페이지 3곳의 링크 일괄 교체 필요.
5. **GA4 측정 ID** — placeholder 상태 지속 (스니펫 준비 완료, ID 없으면 미로드로 안전).

## 다음 검사 권장 시점

- 챗봇 커스텀 도메인 이전 직후 (링크 3곳 재검)
- 접수 서버 연동 직후 (동의 증적 저장·입력 검증 재검)
- 이후 월 1회 정기 (`/검사` 호출)
